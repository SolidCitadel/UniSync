# ========================================
# UniSync 환경변수 설정 예시
# ========================================
#
# 용도:
# - Docker Compose 실행 시 사용
# - E2E 통합 테스트 시 사용
#
# 사용법:
# 1. cp .env.example .env
# 2. 아래 값들을 실제 환경에 맞게 수정
#
# 참고:
# - 로컬 IDE 실행은 각 서비스의 application-local.yml 사용
# - Cognito 값은 LocalStack 초기화 시 자동 생성 (.localstack-outputs.yml 참조)
# ========================================

# Database Configuration (Docker Compose용)
MYSQL_ROOT_PASSWORD=root_password
MYSQL_USER=unisync
MYSQL_PASSWORD=unisync_password

# Database Names (E2E 테스트 및 MySQL 초기화 스크립트용)
USER_SERVICE_DB_NAME=user_db
COURSE_SERVICE_DB_NAME=course_db
SCHEDULE_SERVICE_DB_NAME=schedule_db

# LocalStack Configuration
LOCALSTACK_HOST=localhost
LOCALSTACK_PORT=4566
LOCALSTACK_AUTH_TOKEN=your-localstack-pro-token-here
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Cognito Configuration (LocalStack에서 자동 생성됨)
COGNITO_USER_POOL_ID=ap-northeast-2_xxxxx
COGNITO_CLIENT_ID=xxxxx
COGNITO_REGION=ap-northeast-2
COGNITO_ENDPOINT=http://localhost:4566

# SQS Queue Names
SQS_USER_TOKEN_REGISTERED_QUEUE=user-token-registered-queue
SQS_COURSE_ENROLLMENT_QUEUE=course-enrollment-queue
SQS_ASSIGNMENT_SYNC_NEEDED_QUEUE=assignment-sync-needed-queue
SQS_ASSIGNMENT_EVENTS_QUEUE=assignment-events-queue
SQS_SUBMISSION_EVENTS_QUEUE=submission-events-queue
SQS_TASK_CREATION_QUEUE=task-creation-queue
SQS_DLQ_QUEUE=dlq-queue

# S3 Bucket Names
S3_ATTACHMENTS_BUCKET=unisync-attachments
S3_LAMBDA_CODE_BUCKET=unisync-lambda-code

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=86400000

# Canvas API
CANVAS_API_BASE_URL=https://khcanvas.khu.ac.kr/api/v1
CANVAS_BASE_URL=https://khcanvas.khu.ac.kr
CANVAS_API_TOKEN=your-canvas-api-token-here

# Encryption Configuration
ENCRYPTION_KEY=your-base64-encoded-encryption-key-here

# Service API Keys
CANVAS_SYNC_API_KEY=your-canvas-sync-api-key-here
LLM_LAMBDA_API_KEY=your-llm-lambda-api-key-here

# LLM Configuration
OPENAI_API_KEY=your-openai-api-key-here
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=your-openai-api-key-here
