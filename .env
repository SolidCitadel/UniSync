# ========================================
# UniSync Docker Compose 기본 환경변수
# ========================================
#
# 용도:
# - docker-compose.yml의 인프라 서비스 (MySQL, LocalStack) 기본 설정
# - 모든 docker-compose 파일이 자동으로 로드
#
# 참고:
# - 이 파일은 커밋됨 (민감 정보 없음)
# - 민감 정보는 .env.local에 관리 (gitignore)
# - 각 환경에서 .env.acceptance, .env.demo 등으로 오버라이드 가능
# ========================================

# Database Configuration (기본값, 환경별로 오버라이드 가능)
MYSQL_ROOT_PASSWORD=root_password
MYSQL_USER=unisync
MYSQL_PASSWORD=unisync_password

# Database Names
USER_SERVICE_DB_NAME=user_db
COURSE_SERVICE_DB_NAME=course_db
SCHEDULE_SERVICE_DB_NAME=schedule_db

# LocalStack Configuration (비밀 아닌 설정만)
LOCALSTACK_HOST=localhost
LOCALSTACK_PORT=4566
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
