spring:
  application:
    name: course-service-test

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test

logging:
  level:
    com.unisync.course: DEBUG
    org.hibernate.SQL: DEBUG