# Local Development Profile Example
#
# 사용법:
# 1. 이 파일을 application-local.yml로 복사
# 2. 실제 값으로 변경
# 3. IDE에서 Active Profile을 'local'로 설정하거나
#    SPRING_PROFILES_ACTIVE=local 환경변수 설정
#
# 주의: application-local.yml은 .gitignore에 포함되어 커밋되지 않습니다

# Service URLs (Backend Services)
services:
  user-service:
    url: http://localhost:8081
  course-service:
    url: http://localhost:8082
  schedule-service:
    url: http://localhost:8083

# AWS Cognito Configuration (LocalStack)
aws:
  cognito:
    user-pool-id: ap-northeast-2_0ba219e6ff914508879b28f3de320169  # LocalStack에서 생성된 ID로 변경
    region: ap-northeast-2
    endpoint: http://localhost:4566

# JWT 검증 제외 경로는 application.yml에 정의됨
# (로컬 환경에서도 동일하게 적용)

# Logging
logging:
  level:
    com.unisync: DEBUG
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: INFO
